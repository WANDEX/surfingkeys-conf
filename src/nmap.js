// Essentially only for unmapping default keys after reassigning in keys.js
// for keys which could not be unmapped in keys.js because original key
// is used as 'map' and redefined as 'alias'

// almost all keys [re]defined in keys.js

unmap("ab") // add bookmark -> ;ab

unmap("gxt") // Close tab on left  -> gxL
unmap("gxT") // Close tab on right -> gxR

unmap("gf") // Open a link in non-active new tab -> F

unmap("j") // Scroll down  -> n
unmap("k") // Scroll up    -> e
unmap("l") // Scroll right -> i

unmap("ob") // Open Search with alias b
unmap("og") // Open Search with alias g
unmap("od") // Open Search with alias d
unmap("ow") // Open Search with alias w
unmap("oy") // Open Search with alias y
